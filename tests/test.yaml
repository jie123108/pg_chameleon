---
replica_batch_size: 1000
copy_max_memory: 300M
copy_mode: 'file'
hexify: 
    - 'blob'
    - 'tinyblob'
    - 'mediumblob'
    - 'longblob'
    - 'binary'

 
pid_dir: '~/.pg_chameleon/pid/'
log_dir: '~/.pg_chameleon/logs/'

log_level: 'debug'
log_dest: 'file'
log_append: True
#seconds between each replica loop
sleep_loop: 10 

#Pause the replica for the given amount of seconds if a reindex process is found
pause_on_reindex: Yes
sleep_on_reindex: 30


#list the application names which are supposed to reindex the database
reindex_app_names:
    - 'reindexdb'
    - 'my_custom_reindex'

connections:
  test:
    src_conn:
        host: localhost
        port: 3306
        user: usr_test
        passwd: test
        replica_slot: 100
        replica_database:  db_test
        src_schema: #if type is mysql should be the database to replicate from
        my_charset: 'utf8'
        type: 'mysql' #currently only mysql is supported

    dest_conn:
        host: test
        port: 5432
        user: usr_test
        password: test
        pg_database: db_test
        pg_charset: 'utf8'
        destination_schema: sch_test
    
